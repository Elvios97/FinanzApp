{
  "name": "meingeld-finanzapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:install": "playwright install",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "vite": "^8.0.12"
  }
}
